
config
├── CorsConfig.java      # 跨域全局配置
└── WebmvcConfig.java    # SpringMVC 全局Web配置
module
├── ai        # AI 智能模块
├── member    # 会员模块
├── order     # 订单模块
├── product   # 商品模块
├── recipe    # 食谱核心模块
├── search    # 搜索模块
├── social    # 社交模块
├── upload    # 文件上传模块
└── user      # 用户模块


新增pom文件依赖：
mybatisplus：
<dependency>
<groupId>com.baomidou</groupId>
<artifactId>mybatis-plus-boot-starter</artifactId>
<version>3.5.7</version>
</dependency>

mysql驱动：
<dependency>
<groupId>com.mysql</groupId>
<artifactId>mysql-connector-j</artifactId>
</dependency>
</dependencies>

改动：user的所有代码已经转移到moudle——>user
     新增application.properties
      删除原有的.yaml .yml文件
      原有的user类已经使用mybatisPlus生成器完整生成
      新增登录的功能 user文件夹下
      对应vue页面写完  AI生成 功能测试成功 