<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jcx.jiachangcai.module.social.follow.module.mapper.FollowMapper">

    <select id="getFollowing" resultType="com.jcx.jiachangcai.module.user.entity.User">
        SELECT u.user_id, u.nick_name, u.avatar
        FROM follow f
        JOIN user u ON f.followee_id = u.user_id
        WHERE f.follower_id = #{followerId}
        ORDER BY f.create_time DESC
    </select>

    <select id="getFollowers" resultType="com.jcx.jiachangcai.module.user.entity.User">
        SELECT u.user_id, u.nick_name, u.avatar
        FROM follow f
        JOIN user u ON f.follower_id = u.user_id
        WHERE f.followee_id = #{followeeId}
        ORDER BY f.create_time DESC
    </select>

</mapper>
